/** @format */

import { ModalContent, ModalContentProps } from 'semantic-ui-react'

// TODO Replace with custom ModalContent. Want to eventually move away from Semantic UI
export default function SPModalContent(props: ModalContentProps) {
  return <ModalContent {...props} />
}
