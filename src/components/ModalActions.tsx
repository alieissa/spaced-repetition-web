/** @format */

import { ModalActions, ModalActionsProps } from 'semantic-ui-react'

// TODO Replace with custom ModalActions. Want to eventually move away from Semantic UI
export default function SPModalActions(props: ModalActionsProps) {
  return <ModalActions {...props} />
}
