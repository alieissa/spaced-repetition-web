/** @format */

import { ModalHeader, ModalHeaderProps } from 'semantic-ui-react'

// TODO Replace with custom ModalHeader. Want to eventually move away from Semantic UI
export default function SPModalHeader(props: ModalHeaderProps) {
  return <ModalHeader {...props} />
}
